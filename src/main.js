// Entry point — UI scaffold, screens wired up in later implementation steps
document.querySelector('#app').innerHTML = `
  <div class="app">
    <h1>📷 Foto-Bereinigung</h1>
    <p>Wird implementiert...</p>
  </div>
`
